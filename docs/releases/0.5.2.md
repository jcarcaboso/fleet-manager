# Fleet Manager 0.5.2

This client release lets Fleet claim an existing zero-byte managed file. Empty
files can now be replaced by the desired agent instructions, while non-empty
files that Fleet does not own still produce an ownership conflict.

## Upgrade

Upgrade `fleet-agent` on Nodes that need this behavior. The Server remains
protocol-compatible and does not require a database migration.
